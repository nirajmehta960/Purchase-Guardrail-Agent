# -----------------------------------------------------------------------------
# Core Data Processing
# -----------------------------------------------------------------------------
pandas>=2.1.4
duckdb>=1.0.0
numpy>=1.26.2
flask # maybe remove after testing
scipy 

# Vector Embeddings
sentence-transformers
psycopg2-binary
sqlalchemy
# -----------------------------------------------------------------------------
# Google Cloud Platform
# -----------------------------------------------------------------------------
google-cloud-storage>=2.14.0
google-auth>=2.25.2
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
kagglehub>=0.1.0

# -----------------------------------------------------------------------------
# Configuration Management
# -----------------------------------------------------------------------------
python-dotenv>=1.0.0
pyyaml>=6.0.1

# Latest Airflow version
# apache-airflow is intentionally excluded — it is provided by the runtime
# Airflow VM environment. Install it explicitly in CI (see datapipeline_ci.yml).
# apache-airflow>=3.0.0

# Airflow providers (runtime DAG dependencies — must be present on the Airflow VM)
apache-airflow-providers-standard  # BashOperator, PythonOperator, TriggerDagRunOperator
apache-airflow-providers-smtp      # EmailOperator
apache-airflow-providers-slack     # SlackWebhookOperator

# Airflow GCP provider for GCS integration
apache-airflow-providers-google>=3.1.7

# -----------------------------------------------------------------------------
# Data Version Control (DVC)
# -----------------------------------------------------------------------------
dvc>=3.37.0
dvc-gs>=3.0.1  # GCS remote storage support for DVC

# -----------------------------------------------------------------------------
# Data Validation & Quality
# -----------------------------------------------------------------------------
great-expectations>=0.18.8,<1.0.0
jsonschema>=4.20.0

# -----------------------------------------------------------------------------
# Model Monitoring & Drift Detection
# -----------------------------------------------------------------------------
evidently>=0.4.13

# -----------------------------------------------------------------------------
# Bias Detection & Fairness
# -----------------------------------------------------------------------------
fairlearn>=0.10.0

# -----------------------------------------------------------------------------
# Machine Learning (for future model development phase)
# -----------------------------------------------------------------------------
scikit-learn>=1.3.2

# -----------------------------------------------------------------------------
# API Requests (for api_loader.py)
# -----------------------------------------------------------------------------
requests>=2.31.0
urllib3>=2.1.0

# NOTE:
# Test and lint dependencies are intentionally excluded from runtime image build.
# Install them via data-requirements-dev.txt for local development/testing.

# -----------------------------------------------------------------------------
# Logging & Monitoring
# -----------------------------------------------------------------------------
python-json-logger>=2.0.7

# -----------------------------------------------------------------------------
# Utilities
# -----------------------------------------------------------------------------
tqdm>=4.66.1  # Progress bars
tabulate>=0.9.0  # Pretty-print tables